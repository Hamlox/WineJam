/// @description All steps are belong to us

// handle logic for dragging block and offset
if (isDragged) {

	x = mouse_x + dragOffset_x;
	y = mouse_y + dragOffset_y;

}

// change the state of the person if the object has the token
if (hasToken) {

	// do whichever action is associated with this command block

}